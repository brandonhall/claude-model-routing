{
  "name": "model-routing",
  "description": "Use the right model for the job: a Sonnet session by default, ten assistants pinned to the cheapest model that does each job (one of them the expensive model, on call for decisions), a Sonnet default for any subagent that names none, and a standing note on when to reach up and when to hand down.",
  "version": "2.3.3"
}
